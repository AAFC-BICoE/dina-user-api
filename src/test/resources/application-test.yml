spring:
  datasource:
    username: web_user
    password: test
  liquibase:
    liquibase-schema: dina_user
    user: migration_user
    password: test
embedded.postgresql:
  enabled: true
  image: postgres:12.4
  init-script-file: init-user-module.sql
  database: dina_user_test
  schema: dina_user
